name: Run Salmiac CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  call-salmiac-build-solution-workflow:
    uses: fortanix/salmiac/.github/workflows/run-salmiac-ci.yml@main
    secrets: inherit